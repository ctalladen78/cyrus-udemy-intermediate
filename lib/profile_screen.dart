import 'package:flutter/material.dart';
// TODO build profile screen using preferences
// TODO information and progress graphs
// https://firebase.google.com/docs/reference/js/firebase.User#updateProfile
// currentUser.updateProfile(userUpdateInfo)
// currentUser.updatePassword(password)

class PublicProfileScreen extends StatefulWidget {
  _PublicProfileScreenState createState() => _PublicProfileScreenState();
}

class _PublicProfileScreenState extends State<PublicProfileScreen> {
  @override
  Widget build(BuildContext context) {
    return Container(
       child: Container(),
    );
  }
}